package com.basemethods;

public interface Web2 
{
	// you cannot create object of interface
	// We cannot have non-static variables in this interface
	//in interface we can have static  method
	// we can declare non static method but we cannot have body.
	
	 void m1();   // method declaration or defination
	
	 void m2();
	
	


}
